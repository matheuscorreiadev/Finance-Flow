
import React from "react";
import WasteCard from "./WasteCard";
import AnimatedContainer from "./AnimatedContainer";

interface WasteCategoryInfo {
  title: string;
  color: string;
  description: string;
  examples: string[];
}

const categories: WasteCategoryInfo[] = [
  {
    title: "Paper",
    color: "paper",
    description: "Clean and dry paper products",
    examples: ["Newspapers", "Magazines", "Cardboard", "Office paper"],
  },
  {
    title: "Plastic",
    color: "plastic",
    description: "Certain plastic containers with recycling symbols",
    examples: ["Bottles", "Containers", "Packaging"],
  },
  {
    title: "Glass",
    color: "glass",
    description: "Clean glass containers without lids",
    examples: ["Bottles", "Jars", "Containers"],
  },
  {
    title: "Metal",
    color: "metal",
    description: "Clean metal items",
    examples: ["Cans", "Aluminum foil", "Metal containers"],
  },
  {
    title: "Organic",
    color: "organic",
    description: "Food waste and biodegradable materials",
    examples: ["Food scraps", "Coffee grounds", "Plant trimmings"],
  },
  {
    title: "Landfill",
    color: "landfill",
    description: "Items that cannot be recycled or composted",
    examples: ["Styrofoam", "Certain plastics", "Contaminated materials"],
  },
];

const WasteCategories: React.FC = () => {
  return (
    <section id="categories" className="py-16">
      <AnimatedContainer animationType="slide-up">
        <div className="text-center mb-12">
          <span className="inline-block px-3 py-1 bg-primary/10 text-primary rounded-full text-sm font-medium mb-3">
            Know Your Waste
          </span>
          <h2 className="text-3xl font-bold mb-3">Waste Categories</h2>
          <p className="text-muted-foreground max-w-2xl mx-auto">
            Understanding different waste categories helps ensure proper disposal
            and maximizes recycling efficiency.
          </p>
        </div>
      </AnimatedContainer>

      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {categories.map((category, index) => (
          <WasteCard
            key={category.title}
            title={category.title}
            color={category.color}
            description={category.description}
            delay={100 + index * 100}
          >
            <div className="mt-3">
              <p className="text-xs font-medium mb-1">Examples:</p>
              <div className="flex flex-wrap gap-1">
                {category.examples.map((example) => (
                  <span
                    key={example}
                    className="text-xs px-2 py-1 bg-secondary rounded-full"
                  >
                    {example}
                  </span>
                ))}
              </div>
            </div>
          </WasteCard>
        ))}
      </div>
    </section>
  );
};

export default WasteCategories;
